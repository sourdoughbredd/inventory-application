extends layout 

block content
  h1 #{title}

  form(method="POST")
    div.form-group
      label(for='beer') Beer
      select(name='beer' required)
        option(value='') --Please select a beer--
        for beer in beers
          if selected_beer == beer._id.toString()
            option(value=beer._id, selected) #{beer.name} - #{beer.brewery.name}
          else
            option(value=beer._id) #{beer.name} - #{beer.brewery.name}
    h3 Packaging
    div.form-group
      label(for="packaging_type") Type
      input(type="text" name="packaging_type" required placeholder="Can, Bottle, Keg, etc." value=(beer === undefined || beer.packaging === undefined ? "" : beer.packaging.type))
    div.form-group
      label(for="packaging_volume_oz") Volume (oz)
      input(type="number" name="packaging_volume_oz" min=0.1 step=0.1 required placeholder="12" value=(beer === undefined || beer.packaging === undefined ? "" : beer.packaging.volume_oz))
    div.form-group
      label(for="packaging_quantity") Quantity in Packaging
      input(type="number" name="packaging_quantity" min=1 step=1 required placeholder=30 value=(beer === undefined || beer.packaging === undefined ? "" : beer.packaging.quantity))
    h3 Price and Stock
    div.form-group
      label(for="price") Price
      input(type="number" name="price" min=0 step=0.01 required placeholder=19.99 value=(beer === undefined ? "" : beer.price))
    div.form-group
      label(for="stock") Stock Count
      input(type="number" name="stock" min=0 step=1 required placeholder=20 value=(beer === undefined ? "" : beer.stock))

    
    button(type="submit") Submit

  if errors
    h3 Please fix the following errors: 
    ul
      for error in errors 
        li!= error.msg
